Follow-up Timing Rules:

- Wait 3 days between each follow-up
- Only send follow-up if no reply detected
- Stop all automation if reply is detected

Reply Detection:

- Gmail label: "Podcast Replies"
- Applied manually when a reply is received

This ensures:
- Clean control
- No accidental spam behavior
